## 1. 过滤算法实现与整合

- [x] 1.1 在 `src/twitter_sync.py` 中引入 `datetime` 模块，编写推文发布日期解析和比较逻辑，设置 2026.06.01 阈值。
- [x] 1.2 编写推文原创与自转校验逻辑（比对 URL 链接中提取的用户名与社团配置的 X 账户名）。
- [x] 1.3 编写动态智能关键字生成与匹配函数，提取当前社团的名字、作者、排位和摊位号，并适配繁简体东/東等多样写法。
- [x] 1.4 整合过滤条件到 `scrape_twitter_profile` 中，确保没有配图的推文直接跳过，并支持遇到过期推文时提前终止滚动。

## 2. 接口适配与数据同步优化

- [x] 2.1 调整 `sync_circle_twitter` 函数，使其将当前社团的数据库字典记录传递给 `scrape_twitter_profile` 用于动态生成关键字。
- [x] 2.2 在 `sync_all_circles_twitter` 抓取循环中增加容错与重试，确保单个社团网络错误时不中断整体同步。

## 3. 测试与验证

- [x] 3.1 编写单元测试验证时间解析比较和原创自转提取算法的正确性。
- [x] 3.2 运行实际社团抓取测试，观察日志输出，确保能正常且精准地过滤无关推文。

