## 1. 数据库建表与数据导入模块开发

- [x] 1.1 在 `src/db.py` 或新建 `src/cp31_importer.py` 中编写 `cp31_products` 和 `cp31_circles` 的 SQLite 建表逻辑
- [x] 1.2 编写 JSON 文件解析器，迭代读取 day1data 和 day2data 目录下的 JSON，提取制品和社团信息
- [x] 1.3 编写清洗逻辑，对 `theme_alias` 进行拼写标准化（合并拼写变体与中日文 IP 对照）
- [x] 1.4 实现批量插入与内存级去重，确保数据入库的安全与幂等性

## 2. 比较指标计算引擎实现

- [x] 2.1 编写 `src/cp31_analyzer.py`，实现媒介类型占比（漫画、小说、图集、合志、海报等）的计算逻辑
- [x] 2.2 实现特殊实体属性（无料比例、合志比例、再录比例）的正则与子串词频提取逻辑
- [x] 2.3 实现题材集中度（$CR_5$ 与 $CR_{10}$）及对应贝恩分类的经济学统计逻辑
- [x] 2.4 实现基于 `hotCount` 归一化大盘的真实供需偏离度（Real-time DBI）计算逻辑
- [x] 2.5 编写双日重合度与空间莫兰自相关指数计算，对比中日两展的时空排布特征

## 3. 命令行接口扩展与整合

- [x] 3.1 在 `main.py` 中新增 `--import-cp31 <dir>` 参数，并挂接导入逻辑函数
- [x] 3.2 在 `main.py` 中新增 `--analyze-cp31` 参数，挂接计算与报告生成逻辑函数
- [x] 3.3 编写对应的单元测试，验证导入命令和计算命令在测试数据库中的功能完整性

## 4. 比较分析报告生成与数据验证

- [x] 4.1 在分析命令中编写报告生成模块，自动将指标对比数据与 Mermaid 流程图输出至 `research/comiket_vs_comicup_comparison.md`
- [x] 4.2 运行完整分析链条，生成中日对比报告，并执行 `uv run pytest` 回归测试确保系统代码无破坏
